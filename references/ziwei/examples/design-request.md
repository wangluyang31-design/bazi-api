# 示例：设计型请求

## 用户请求
请帮我把紫微斗数做成一个给 Claude Code 和 Codex 用的 Skill，要有 schema、规则结构、输出模板。

## 期望行为
- 识别为 `design`
- 输出 input schema、derived features、deterministic rules、interpretive rules、decision flow
- 给出可安装的技能包结构
- 不虚构任何个人命盘
